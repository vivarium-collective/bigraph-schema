[project]
name = "bigraph-schema"
version = "0.0.65"
description = "A serializable type schema for compositional systems biology"
readme = "README.md"
requires-python = ">=3.7"
authors = [
  {name = "Eran Agmon"},
  {name = "Ryan Spangler"}
]
dependencies = [
    "fire",
    "numpy",
    "orjson",
    "parsimonious",
    "pint",
    "plum-dispatch",
    "pytest",
    "ipdb",
    "requests>=2.31.0",
]

[tool.setuptools]
packages = [
    "bigraph_schema"
]
